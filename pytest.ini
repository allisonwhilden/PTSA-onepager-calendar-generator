[pytest]
# python/ so `from calendar_gen import ...` resolves the way build.py resolves
# it; the repo root so `from web import ...` does too.
pythonpath = python .
testpaths = python/tests web/tests
addopts = -q
